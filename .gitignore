Nothing needs to be added to .gitignore since the only change is a modification to a batch file (SETUP_JARVIS.bat), which is a source/config file and not a build artifact, dependency, or temporary file.